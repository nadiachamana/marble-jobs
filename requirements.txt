# Web framework
fastapi==0.136.3
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20

# Data
sqlalchemy==2.0.36
pydantic==2.13.4
pydantic-settings==2.7.1

# Postgres driver (prod on Railway); SQLite is built into Python for dev
psycopg2-binary==2.9.10

# HTTP + Ashby API / Slack webhook
httpx==0.28.1

# Claude (inference + form classification)
anthropic==0.111.0

# Browser automation for posting engines
playwright==1.49.1

# Email (smtplib is stdlib; this is for nicer message building only)
# -- no extra dep needed --
